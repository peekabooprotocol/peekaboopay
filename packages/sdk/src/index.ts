export { PASClient } from "./client";

// Re-export types for convenience
export type {
	PayParams,
	PaymentReceipt,
	ReceiveParams,
	ReceiveAddress,
	SwapParams,
	SwapReceipt,
	BridgeParams,
	BridgeReceipt,
	DisclosureRequest,
	Credential,
	PolicyRule,
	PASResult,
	AgentConfig,
} from "@pas/types";
